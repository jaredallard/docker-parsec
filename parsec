#!/usr/bin/env bash
#
# Start the docker container shit

set -e -x 

USERID="$(id -u)"
xhost "si:localhost:${USER}"

docker run -it --rm -e DISPLAY=$DISPLAY -v /tmp/.X11-unix:/tmp/.X11-unix \
 -v "/run/user/$USERID/pulse:/run/user/$USERID/pulse:ro" --device=/dev/dri:/dev/dri \
 jaredallard/parsec
